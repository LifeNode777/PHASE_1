# MODULE A — FALSIFICATION CONDITIONS

Module A is considered **failed** if:

1. It fails to detect K1/K2 motifs in the *Pleurotus ostreatus* signal with
   SNR ≥ 20 dB (validation against the Adamatzky et al. protocol, bioRxiv 2026).
2. It introduces discretization (ADC) into the coupling loop, resulting in
   loss of phase coherence (θ drops below 0.70 in a comparative test: fully
   analog path vs. discretized path, on the same biological source).
3. It fails to maintain thermal stability in the 20–40°C range for ≥ 72 h
   (baseline drift > 10% of the K1/K2 signal amplitude is disqualifying).

**Operational interpretation of test #2:** the same biological source is run
in parallel through the analog path A→Q-Core and a path with ADC; the
Takens-reconstructed trajectories from both paths are compared with the θ
metric. The analog path must maintain θ ≥ 0.70 where the discretized path
degrades — otherwise Module A does not fulfill its ontological function.

Negative results are results. They will be published with the same DOI
discipline as positive ones.
